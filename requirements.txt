# Production dependencies for xrpldashboard.
# Pinned to match local venv versions for reproducibility.
# Update intentionally; don't drift unless you've tested.

Flask==3.1.3
Flask-Limiter==3.8.0
xrpl-py==4.5.0
gunicorn==23.0.0
psycopg[binary]==3.3.4
