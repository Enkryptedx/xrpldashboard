# a1 deploy paste — 2026-08-16 (Charlie's next keyboard moment)

## What ships
Registry-driven anchor canary. Alerts on hashes we published missing
from every node we can reach — not on rippled's retention window.

| Change | File | Lines |
|---|---|---|
| NEW | `docs/anchor_registry.json` | 30 (backfilled anchors #1 + #2) |
| REWRITE | `tools/anchor_canary.py` | 605 → 809 (v1.0 → v2.0) |
| NEW | `tools/anchor_registry_append.py` | 209 (ceremony post-stamp tool) |
| REWRITE | `tests/test_anchor_canary.py` | 301 → 421 (13 tests, all green on 3.13) |

## Systemd — nothing to reinstall
Diffed `deploy/anchor_canary/*.{service,timer}` against `/etc/systemd/system/*` — identical. The service invokes `/home/charlie/xrpldashboard/tools/anchor_canary.py`; swapping the script picks up on next timer fire. No `systemctl daemon-reload`, no unit edit.

## Env vars — no new required
Canary reads `ANCHOR_CANARY_REGISTRY_PATH` if set (defaults to `<repo>/docs/anchor_registry.json`). Default resolution works because the script lives in `tools/` and walks up one dir. Env file at `/home/charlie/.config/xrpldashboard/env` needs no edit.

## Deploy (from Mac terminal, Charlie's sudo not needed)

```bash
# 1) Push the 4 files to Lenovo. Copy-verify happens next step.
scp /Users/charliebruce/xrpl_test/tools/anchor_canary.py \
    charlie@192.168.40.95:/home/charlie/xrpldashboard/tools/anchor_canary.py

scp /Users/charliebruce/xrpl_test/tools/anchor_registry_append.py \
    charlie@192.168.40.95:/home/charlie/xrpldashboard/tools/anchor_registry_append.py

scp /Users/charliebruce/xrpl_test/docs/anchor_registry.json \
    charlie@192.168.40.95:/home/charlie/xrpldashboard/docs/anchor_registry.json

scp /Users/charliebruce/xrpl_test/tests/test_anchor_canary.py \
    charlie@192.168.40.95:/home/charlie/xrpldashboard/tests/test_anchor_canary.py

# 2) Sanity-verify on Lenovo — expect all 4 present, expected sizes.
ssh charlie@192.168.40.95 'ls -la \
    /home/charlie/xrpldashboard/tools/anchor_canary.py \
    /home/charlie/xrpldashboard/tools/anchor_registry_append.py \
    /home/charlie/xrpldashboard/docs/anchor_registry.json \
    /home/charlie/xrpldashboard/tests/test_anchor_canary.py'

# 3) Dry-run the canary on Lenovo. Expect a 🟢 heartbeat with
#    "0/2 verified via local sovereign node, 2/2 verified via public
#    witness" (that is the honest-green under online_delete=10000 —
#    both anchors are older than the ~13.5h local retention).
ssh charlie@192.168.40.95 'cd /home/charlie/xrpldashboard && \
    set -a; source /home/charlie/.config/xrpldashboard/env; set +a; \
    /home/charlie/xrpldashboard/.venv/bin/python -u \
      tools/anchor_canary.py --dry-run --force-heartbeat'
```

## Verify Tuesday's first honest-green
Timer next fires on `OnCalendar=daily` (roughly 24h from last fire). Tuesday's 09:00-10:00 ET window will additionally emit the weekly heartbeat. Both should read 🟢 with the local/witness split. If not, page yourself — that's a real signal now, not noise.

## No rollback plan needed
Old canary lives at zero paths in git (untracked on both machines). If the new one misbehaves, the fallback is to move it aside — the systemd unit fails-quiet (silent for one cycle, then next scheduled fire), no user-facing outage. Nothing on the site depends on the canary.

## Git commit (separate concern, Charlie's choice)
All 4 files are `??` on both Mac + Lenovo. When you're ready:

```bash
cd /Users/charliebruce/xrpl_test
git add tools/anchor_canary.py tools/anchor_registry_append.py \
        docs/anchor_registry.json tests/test_anchor_canary.py \
        deploy/anchor_canary/DEPLOY_A1_2026-08-16.md
git commit -m "anchor canary v2.0 — registry-driven cascade

Prior design fired latest_anchor_unavailable ~6/7 daily runs because
account_tx ledger_index_min=-1 scopes to the online_delete=10000
~13.5h window while anchors land weekly. Rewrite iterates our own
docs/anchor_registry.json and looks each hash up via tx RPC (local
sovereign first, public witness on notFound). 13/13 tests pass."
```

Push is your call.

## Next anchor ceremony (Friday, anchor #3)
After stamping via Xaman, one command records the hash into the
registry AND immediately verifies via the canary:

```bash
ssh charlie@192.168.40.95 'cd /home/charlie/xrpldashboard && \
    set -a; source /home/charlie/.config/xrpldashboard/env; set +a; \
    /home/charlie/xrpldashboard/.venv/bin/python -u \
      tools/anchor_registry_append.py --tx-hash <NEW_HASH>'
```

Refuses to append if: hash already registered, tx not validated,
TransactionResult != tesSUCCESS, Account mismatch, no v1 memo. On
success prints the new registry row and confirms canary green.
