# Test / CI tooling only — NOT installed by Render's prod build
# (render.yaml runs `pip install -r requirements.txt` alone, so prod stays
# lean). CI installs this file on top of requirements.txt.
#
# Added 2026-09-25 (item 4): the first CI run went red with
# "No module named pytest" — pytest was never in requirements.txt; it only
# existed in the local dev venv. That is exactly the environment/finding
# CI is meant to surface. Pinned to the version the local venv proved
# against.
-r requirements.txt
pytest==9.0.3
