# pybabel extraction config — tells `pybabel extract` where to find _()
# calls in our codebase. Templates use the Jinja2 extractor, Python uses
# the standard extractor. Keep extensions enabled minimal — we don't need
# any non-standard ones.
[python: **.py]
[jinja2: templates/**.html]
extensions=jinja2.ext.i18n
